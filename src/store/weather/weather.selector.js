// import { createSelector } from "reselect";

// const selectWeatherReducer = (state) => state.weather;

// export const selectCity = createSelector(
//   [selectWeatherReducer],
//   (weather) => weather.city
// );
